# Database (dummy-ready)

- `schema.sql` — tables: `products, templates, inquiries, blog_posts`
- `seed.sql` — sample rows (added when backend goes live)
- No connection yet. Backend will use `DATABASE_URL` from `backend/.env`.
